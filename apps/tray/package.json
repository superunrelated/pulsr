{
  "name": "@pulsr/tray",
  "version": "0.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "devDependencies": {
    "electron": "^33.2.0",
    "typescript": "^5.9.0",
    "@types/node": "^24.0.0"
  }
}
